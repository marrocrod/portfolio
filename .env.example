# Copy to .env.local for local development and add the same keys in
# Vercel > Project > Settings > Environment Variables.
# None of these are needed yet; they are used by later demos.

# Voice assistant
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
HF_TOKEN=

# Rate limiting for paid API routes
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
